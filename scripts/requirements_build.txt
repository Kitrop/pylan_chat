# Зависимости для сборки LANChat с PyInstaller
pyinstaller>=5.0
setuptools>=65.0
wheel>=0.37.0

# Основные зависимости приложения (минимум для сборки)
requests>=2.28.0
fastapi>=0.95.0
uvicorn>=0.20.0

# Дополнительные зависимости для сборки
pywin32>=305; sys_platform == "win32"  # Только для Windows
